# GrowEasy CSV AI Importer

AI-powered CSV importer that maps arbitrary lead-export CSVs into GrowEasy CRM format.

## Status
🚧 In active development.

## Structure
- `frontend/` — Next.js upload + preview UI
- `backend/` — Express API + AI extraction pipeline